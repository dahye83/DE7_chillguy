CREATE OR REPLACE TABLE TEAM_7_CHILL.GOLD.GD_ROUTE_AIRLINE_SHARE AS
SELECT DESTINATION, AIRLINE, COUNT(*) AS FLIGHT_CNT
FROM SILVER.SL_AIRPORT_SCHEDULE_STATIC_YEARLY
GROUP BY DESTINATION, AIRLINE
ORDER BY 1, 3 DESC;