CREATE OR REPLACE TABLE TEAM_7_CHILL.GOLD.GD_NATION_AIRLINE_SHARE AS
SELECT NATION_NM_KOR
	 , AIRLINE
	 , COUNT(*) as FLIGHT_CNT
FROM SILVER.SL_AIRPORT_SCHEDULE_STATIC_YEARLY_JOINED
GROUP BY 1, 2
ORDER BY 1, 3 DESC;
