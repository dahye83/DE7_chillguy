CREATE OR REPLACE TABLE TEAM_7_CHILL.GOLD.GD_DELAY_REASON_CNT AS
SELECT DELAY_REASON, COUNT(*) as DELAY_REASON_CNT
FROM SILVER.SL_AIRPORT_SCHEDULE_STATIC_YEARLY
WHERE DELAY_REASON IS NOT NULL
GROUP BY DELAY_REASON
ORDER BY 2 DESC;